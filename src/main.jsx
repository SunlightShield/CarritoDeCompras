import React from 'react'
import ReactDOM from 'react-dom/client'
import {CartApp} from "../src/CartApp"


ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <CartApp />
  </React.StrictMode>,
)
